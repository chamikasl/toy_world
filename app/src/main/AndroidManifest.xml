<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.toy_world"
        tools:replace="android:theme">

        <activity android:name="com.icbt.toy_world.User.HomeActivity" />
        <activity android:name="com.icbt.toy_world.User.SettingsActivity" />
        <activity android:name="com.icbt.toy_world.User.SearchProductsActivity" />
        <activity android:name="com.icbt.toy_world.RegisterActivity" />
        <activity android:name="com.icbt.toy_world.User.ProductDetailsActivity" />
        <activity android:name="com.icbt.toy_world.LoginActivity" />
        <activity android:name="com.icbt.toy_world.User.ConfirmFinalOrderActivity" />
        <activity android:name="com.icbt.toy_world.User.CartActivity" />
        <activity android:name="com.icbt.toy_world.Admin.OrderedProductsActivity" />
        <activity android:name="com.icbt.toy_world.Admin.NewOrdersActivity" />
        <activity android:name="com.icbt.toy_world.Admin.AddNewCategoryActivity" />
        <activity android:name="com.icbt.toy_world.Admin.AddNewProductActivity" />
        <activity android:name="com.icbt.toy_world.Admin.MainScreenActivity" />
        <activity android:name="com.icbt.toy_world.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>